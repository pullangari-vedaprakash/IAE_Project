<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Event Manager - Analytics</title>
    <link rel="stylesheet" href="/Events/eventmanager_analytics.css">
</head>
<body>
    <div class="navbar">
        <header>
            <a href="/home" class="badge">Happy Tails</a>
            <div class="menu">
                <a href="/eventmanager_profile" class="nav-link" data-page="Profile">Profile</a>
                <a href="/logout" class="nav-link" data-page="Logout">Logout</a>
            </div>
        </header>
    </div>

    <div class="dashboard-container">
        <div class="sidebar">
            <h2>Dashboard</h2>
            <ul>
                <li><a href="/eventmanager_dashboard" class="nav-link" data-page="Home">Home</a></li>
                <li><a href="/eventmanager_events" class="nav-link" data-page="Events">Events</a></li>
                <li><a href="/eventmanager_attendees" class="nav-link" data-page="Attendees">Attendees</a></li>
                <li><a href="/eventmanager_profile" class="nav-link" data-page="Profile">Profile</a></li>
                <li><a href="/eventmanager_analytics" class="nav-link active" data-page="Analytics">Analytics</a></li>
            </ul>
        </div>

        <div class="main-content">
            <div class="page-container active">
                <div class="title-bar">
                    <h1 class="dashboard-title">Analytics</h1>
                </div>

                <h2 class="section-header">Revenue</h2>
                <div class="analytics-grid">
                    <div class="card">
                        <h2>Revenue Overview</h2>
                        <div class="stat">
                            <div class="stat-label">Total Revenue</div>
                            <div class="stat-value">$<%= revenue.total.toFixed(2) %></div>
                        </div>
                        <div class="stat">
                            <div class="stat-label">Today</div>
                            <div class="stat-value">$<%= revenue.today.toFixed(2) %> <span class="indicator <%= revenue.todayChange >= 0 ? 'up' : 'down' %>"><%= revenue.todayChange >= 0 ? '+' : '' %><%= revenue.todayChange %>%</span></div>
                        </div>
                        <div class="stat">
                            <div class="stat-label">This Week</div>
                            <div class="stat-value">$<%= revenue.thisWeek.toFixed(2) %> <span class="indicator <%= revenue.thisWeekChange >= 0 ? 'up' : 'down' %>"><%= revenue.thisWeekChange >= 0 ? '+' : '' %><%= revenue.thisWeekChange %>%</span></div>
                        </div>
                        <div class="stat">
                            <div class="stat-label">This Month</div>
                            <div class="stat-value">$<%= revenue.thisMonth.toFixed(2) %> <span class="indicator <%= revenue.thisMonthChange >= 0 ? 'up' : 'down' %>"><%= revenue.thisMonthChange >= 0 ? '+' : '' %><%= revenue.thisMonthChange %>%</span></div>
                        </div>
                    </div>
                    <div class="card">
                        <h2>Average Ticket Value</h2>
                        <div class="stat">
                            <div class="stat-label">Overall Average</div>
                            <div class="stat-value">$<%= avgTicketValue.total.toFixed(2) %></div>
                        </div>
                        <div class="stat">
                            <div class="stat-label">Today</div>
                            <div class="stat-value">$<%= avgTicketValue.today.toFixed(2) %> <span class="indicator <%= avgTicketValue.todayChange >= 0 ? 'up' : 'down' %>"><%= avgTicketValue.todayChange >= 0 ? '+' : '' %><%= avgTicketValue.todayChange %>%</span></div>
                        </div>
                        <div class="stat">
                            <div class="stat-label">This Week</div>
                            <div class="stat-value">$<%= avgTicketValue.thisWeek.toFixed(2) %> <span class="indicator <%= avgTicketValue.thisWeekChange >= 0 ? 'up' : 'down' %>"><%= avgTicketValue.thisWeekChange >= 0 ? '+' : '' %><%= avgTicketValue.thisWeekChange %>%</span></div>
                        </div>
                        <div class="stat">
                            <div class="stat-label">This Month</div>
                            <div class="stat-value">$<%= avgTicketValue.thisMonth.toFixed(2) %> <span class="indicator <%= avgTicketValue.thisMonthChange >= 0 ? 'up' : 'down' %>"><%= avgTicketValue.thisMonthChange >= 0 ? '+' : '' %><%= avgTicketValue.thisMonthChange %>%</span></div>
                        </div>
                    </div>
                </div>

                <h2 class="section-header">Attendees</h2>
                <div class="analytics-grid">
                    <div class="card">
                        <h2>Attendee Overview</h2>
                        <div class="stat">
                            <div class="stat-label">Total Attendees</div>
                            <div class="stat-value"><%= attendees.total %></div>
                        </div>
                        <div class="stat">
                            <div class="stat-label">New Today</div>
                            <div class="stat-value"><%= attendees.today %> <span class="indicator <%= attendees.todayChange >= 0 ? 'up' : 'down' %>"><%= attendees.todayChange >= 0 ? '+' : '' %><%= attendees.todayChange %>%</span></div>
                        </div>
                        <div class="stat">
                            <div class="stat-label">New This Week</div>
                            <div class="stat-value"><%= attendees.thisWeek %> <span class="indicator <%= attendees.thisWeekChange >= 0 ? 'up' : 'down' %>"><%= attendees.thisWeekChange >= 0 ? '+' : '' %><%= attendees.thisWeekChange %>%</span></div>
                        </div>
                        <div class="stat">
                            <div class="stat-label">New This Month</div>
                            <div class="stat-value"><%= attendees.thisMonth %> <span class="indicator <%= attendees.thisMonthChange >= 0 ? 'up' : 'down' %>"><%= attendees.thisMonthChange >= 0 ? '+' : '' %><%= attendees.thisMonthChange %>%</span></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>